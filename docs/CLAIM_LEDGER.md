# Claim ledger

Every claim Bullseye makes about itself, what backs it, and what does not. If a claim is not in
the "Verified" table with evidence you can run or open, do not repeat it in a pitch, a README or
a demo. Last reviewed: 20 September 2026.

Status values: **VERIFIED** (evidence in this repo, reproducible) · **PARTIAL** (mechanism built
and tested, but not against the real external system) · **BLOCKED** (cannot be done yet; reason
given) · **NOT CLAIMED**.

Rows V18 to V21 were P1, P2, P3 and B2 until the first live run on 18 September 2026. Each is
scoped to what was observed that day: one investigation and one testnet settlement.

Rows V34 to V44 were added on 19 September 2026, after an outside review of commit `1ae8e79`
found that the browser could not complete or recover a purchase, that the read routes gave away
buyers' orders and most of what a Brief sells, and that the gate's number rule could be passed
by figures with no evidence behind them. Each of those rows is verified by tests that run
offline: recorded data, the fixture synthesiser, the fixture payment rail and a stand-in wallet.
They were merged to `main` as `826820d` and deployed on 19 September 2026 (V43). Deployed means
the public service now runs this code and serves the new read contract. It does not mean any of
it has been exercised there with a wallet extension or on a real rail: nobody has bought from
the deployed address. B5 is the list of what that would take.

Rows V45 to V50 were added on 20 September 2026 with the Market Desk
([MARKET_DESK.md](MARKET_DESK.md)), which reads one verified event as money. Its central finding is
a negative one and is the reason none of those rows claims an opportunity: the sources Bullseye may
read publish no bid, ask, size or expiry for any asset, so no spread can be quoted, and the costs
that would have to be netted against one are unknown. The desk shows that absence by name instead
of estimating around it. They are verified offline, against recorded real responses, except V48,
whose data feasibility was established from live responses the same day.

## Verified

| # | Claim | Evidence |
| --- | --- | --- |
| V1 | Bullseye fetches genuine RWA reference data from the xStocks public API and validates it against strict schemas before use. A record that fails validation is left out and reported, not silently dropped. | `npm run spike` step 1; `apps/api/src/adapters/xstocks.ts`; `artifacts/integration/spike-2026-09-18T21-37-16-168Z.json` (URL, sha256 of the response, 50 actions, none rejected by the schema). The 21:04 UTC run the same day failed this step on a Cancelled record; see V22. |
| V2 | Bullseye connects to X Layer mainnet (chain id 196) and reads contract state, including historical state. | `npm run spike` steps 2 and 3 (step 3 reads `multiplier()` at past blocks); `apps/api/src/adapters/xlayer.ts`; `artifacts/integration/spike-2026-09-18T21-37-16-168Z.json`. |
| V3 | On 18 Sep 2026 the detector flagged real dividend rebases on xStocks deployed on X Layer, and for IFFx and QSRx the on-chain `multiplier()` was the old value 60 s before the issuer's effective time and the new value 60 s after, with activation in the block stamped 00:30:00Z. | `artifacts/recorded/xstocks-2026-09-18/` (unmodified responses with sha256 and fetch time, for both tokens); the IFFx case is replayed in `apps/api/test/transport.test.ts`. The QSRx reads were repeated live in V18. |
| V4 | Detector output is reproducible: identical inputs give identical SignalEvents, including ids and input hashes. | `apps/api/test/detector.test.ts`. |
| V5 | A SignalEvent carries category, asset, observation and detection timestamps, sources and provenance. | `packages/domain/src/signal.ts`; detector test "carries category, asset, …". |
| V6 | Investigations run under a hard ceiling on variable cost, model calls, tool calls and latency, enforced by code before each call; an uneconomic investigation is declined before any spend. | `apps/api/src/research/governor.ts`; `investigation.test.ts` "research budget governor" (5 tests). |
| V7 | The publication gate is deterministic. Its 11 rules reject: an incomplete investigation, a draft that does not match the schema, missing mandatory evidence, stale evidence, claims citing evidence that was never collected, figures or timestamps that are not bound to evidence (V36 says what "bound" means since gate 2.0.0), quantities that disagree with evidence or name the wrong unit, undisclosed failed checks, confidence above the computed cap, investment-advice language, and fixture inputs. | `apps/api/src/gate/publicationGate.ts`, `apps/api/src/gate/numericGrounding.ts`; `gate.test.ts` (15 tests), `gate-grounding.test.ts` (55 tests); `investigation.test.ts` adverse cases. |
| V8 | A rejected or stopped investigation produces no Brief, and nothing can be charged for it. | `investigation.test.ts` "blocks a Brief when …". |
| V9 | The human Brief and the machine JSON are the same object: the UI renders the delivered `bullseye.brief/v1` JSON, whose sha256 content hash is re-checked on every read. | `apps/api/src/briefs.ts`; `investigation.test.ts` first test. |
| V10 | Bullseye's paid endpoint speaks x402 v2 using the OKX seller SDK (`@okxweb3/x402-core` resource server, `ExactEvmScheme`), and the OKX client SDK can parse its challenge, sign it and be served. | `apps/api/src/commerce/`; `checkout.test.ts` (uses the real OKX client SDK as the buyer). Seen once on the real testnet rail: V20. |
| V11 | Order states distinguish QUOTED, PAYMENT_PENDING, PAYMENT_UNKNOWN, PAID, DELIVERING, DELIVERED, PAYMENT_FAILED, DELIVERY_FAILED and RECONCILIATION_REQUIRED; illegal transitions are refused; events are append-only. | `packages/domain/src/order.ts`; `ledger.test.ts`. |
| V12 | One signed authorization maps to one order and is settled at most once, including under replay and concurrent retries; a timeout is recorded as unknown, never as success or failure. | `checkout.test.ts` "replayed", "concurrently", "settle timeout", "dropped settle connection". Seen once with a real facilitator timeout: V26. |
| V13 | Quote terms cannot change after issue: database triggers refuse updates and deletes, the order stores the terms hash, and a mismatch is detected on read. | `apps/api/src/db.ts`; `checkout.test.ts` "keeps quote terms immutable"; `ledger.test.ts` "detects terms that were altered". |
| V14 | Data is labelled LIVE, CACHED, HISTORICAL or FIXTURE by the transport that produced it; a failed live source yields CACHED only when allowed and otherwise an error; nothing falls back to fixtures. | `apps/api/src/adapters/transport.ts`; `transport.test.ts`. |
| V15 | Economics show price, measured usage and costs, and estimated allowances in separate lists with separate totals; testnet and fixture payments are reported as not revenue. | `apps/api/src/economics/receipt.ts`; `checkout.test.ts` last test. |
| V16 | The whole pipeline runs offline from recorded real data with no keys: `npm run start:offline`, then `npm run demo -- IFFx --buy`. | `artifacts/evidence/demo-run-2026-09-18T19-36-43-346Z.json` (data HISTORICAL, synthesis FIXTURE, rail FIXTURE, all stated in the transcript); `artifacts/evidence/delivery-ord_ffea303d6723b151.json`. |
| V17 | In a real browser, the golden path and the failure paths behave as claimed: a gate-rejected draft is never offered for sale; a delivered Brief on screen is a rendering of the delivered JSON and its order carries the terms hash the buyer saw; an unknown payment delivers nothing, offers no second Pay button, and completes after the buyer's own reconciliation with one wallet signature. The buyer-side cases are in V34. | `tests/e2e/golden-path.spec.ts` (7 tests, `npm run test:e2e`; offline configuration, so data HISTORICAL, synthesis and rail FIXTURE; the wallet is a stand-in EIP-1193 provider that signs in the test process, so the page runs its real wallet code and no wallet extension is involved). |
| V18 (was P1) | On 18 Sep 2026 (21:58–22:01 UTC) one investigation ran end to end with a live model and all data LIVE, and the gate published its Brief. Signal `sig_24ecf336903c1da1`: QSRx, corporate action `7b14c6dc-1532-4d64-908f-3c98b05efe94` v2, CashDividend, multiplier 1 → 1.0066516577977895, effective 2026-09-18T00:30:00Z. 10 tool calls, 10 evidence items, 9 of 9 consistency checks PASS. 4 model calls through `openrouter/auto` (cost tier medium, price cap $3/$15 per million tokens), routed to `deepseek/deepseek-v4-pro` and `openai/gpt-5.6-terra`. Brief `brf_a790c648a87d52dd`, dataMode LIVE, confidence cap HIGH. This is one investigation. It says nothing about Brief quality, other events or other routed models. | `artifacts/evidence/demo-run-2026-09-18T22-01-13-583Z.json` (health, signal, timeline, gate findings); the Brief with its evidence, checks and gate result is inside `artifacts/evidence/delivery-ord_8a2102084ad69dab.json`; provider `apps/api/src/research/openrouter.ts`; request shape and failure handling in `apps/api/test/openrouter.test.ts` (22 tests). |
| V19 (was P2) | The model cost of that one investigation was measured, not estimated: $0.071725 for 4 calls, each costed at the amount OpenRouter reported charging (`MEASURED_PROVIDER_BILLED`), against a $0.60 ceiling. No call was carried at an upper bound. It is the cost of one Brief, not an average. | `artifacts/evidence/demo-run-2026-09-18T22-01-13-583Z.json`, step `investigation`: `measuredModelCostUsd: 0.071725`, `upperBoundModelCostUsd: 0`, `budgetSpentUsd: 0.071725`, `costBases: ["MEASURED_PROVIDER_BILLED"]`; the timeline lists each call's routed model, tokens and charge. Cost bases: `packages/domain/src/budget.ts`. |
| V20 (was P3) | On 18 Sep 2026 (22:03 UTC) one purchase of Bullseye's paid endpoint settled through the real OKX facilitator on X Layer testnet (`eip155:1952`) and was verified on-chain. Order `ord_8a2102084ad69dab`, buyer `0xc19E520dFE1A493961Fe7092F6017E1f2BDC50d6` (the agent buyer, `npm run buy -- latest`), 3000000 base units of testnet USD₮0 to `0xa8bcd760a7c280c05090431c6afdf15df324d64a`, transaction `0xa2f4058c4a839f58e3dcdc574f2cd0090d76cffd0848d89cebd32d6cb723f28a`, block 41310183. `npm run verify-payment -- ord_8a2102084ad69dab` read the chain directly: receipt success, quoted Transfer found, from the buyer, verdict VERIFIED. One delivery. This is one settlement, on testnet, with test tokens. It is not revenue and it is not mainnet. | `artifacts/integration/payment-ord_8a2102084ad69dab.json`; `artifacts/evidence/delivery-ord_8a2102084ad69dab.json` (`payment.chainVerified: true`, `chainBlockNumber: 41310183`, quote `quo_7fde34706f1b7eba` and its terms hash); explorer: https://www.oklink.com/x-layer-testnet/tx/0xa2f4058c4a839f58e3dcdc574f2cd0090d76cffd0848d89cebd32d6cb723f28a |
| V21 (was B2) | With OKX facilitator credentials and a `PAY_TO_ADDRESS` configured, the seller rail initialises against the real OKX facilitator on X Layer testnet, and the paid endpoint answers an unpaid request with HTTP 402 and a `PAYMENT-REQUIRED` header: scheme `exact`, network `eip155:1952`, asset `0x9e29b3aada05bf2d2c827af80bd28dc0b9b4fb0c` (USD₮0), amount `3000000`, payTo `0xa8bcd760a7c280c05090431c6afdf15df324d64a`, quote `quo_7fde34706f1b7eba`. Without credentials the OKX SDK cannot initialise (SPONSOR_FEEDBACK SF-4) and the server answers 503 rather than issuing a challenge. | `artifacts/integration/spike-2026-09-18T21-37-16-168Z.json`, step "okx: Bullseye seller rail initialises against the OKX facilitator": PASS, `"ready":true`, `"detail":"facilitator supports exact on eip155:1952"`; `artifacts/evidence/demo-run-2026-09-18T22-01-13-583Z.json`, step `unpaid request`; `checkout.test.ts` "does not issue a challenge when the rail cannot settle". |
| V22 | Cancelled and superseded issuer actions never become signals. A later Cancelled row voids the version it names; a later live row that starts from the same old multiplier replaces the earlier row; a later live row that starts where the earlier one ended is a further rebase and both stand. An event with a row that fails validation is not trusted, and the evidence tool fails closed. If the issuer voids an action after it was flagged, the evidence says so, core check `CHK-ACTION-STILL-CURRENT` fails, confidence is capped at LOW, the stored signal is marked superseded, and its Brief is withdrawn from sale: `410 brief_withdrawn`, no challenge, nothing signed is settled, the catalogue omits it, and a buyer who already paid still gets delivery. Verified by tests on fixture data. No live Brief has been withdrawn. | `apps/api/test/cancelled-actions.test.ts` (12 tests); `apps/api/test/costing-and-withdrawal.test.ts` "a Brief about an action the issuer has voided"; `supersededBy` in `apps/api/src/signals/rebaseDetector.ts`; `apps/api/src/evidence/checks.ts`; `apps/api/src/commerce/checkout.ts`. Live trigger: `artifacts/integration/spike-2026-09-18T21-04-42-798Z.json` (scan failed on a Cancelled record with a null effective time) and `artifacts/integration/spike-2026-09-18T21-37-16-168Z.json` (rerun passes: Initial 47, Corrected 1, Cancelled 2, none rejected). |
| V23 | The agent buyer's spend limits apply to the payment terms it would sign, not to the seller's stated price: network allow-list (`eip155:1952` only unless `--allow-mainnet`), known settlement asset per network, base-unit amount at most `--max-usd` (default 5), scheme `exact`, usable recipient. The tests cover that decision. Registering the signer only for allowed networks, filtering offers, and re-checking the signed payload before it is sent are in the buyer script and are not unit-tested; they ran once, on an acceptable offer, in the purchase in V20. | `scripts/spend-guard.ts`; `apps/api/test/spend-guard.test.ts` (5 tests); `scripts/buy-brief.ts`. |
| V24 | Recorded model spend reconciles with the provider's own ledger for the key used on 18 Sep 2026. OpenRouter's `GET /api/v1/key` reported cumulative usage $0.07222918. Bullseye's recorded calls sum to $0.0722305: the model check $0.0005055 (`z-ai/glm-5.2`, 27 prompt and 68 completion tokens) plus the investigation $0.071725. The $0.0000013 difference is rounding to 6 decimals per record. The provider's ledger lags: it still showed no change 3 s after the model check. Two recorded runs on one key on one day. | `artifacts/integration/model-check-2026-09-18T21-57-37-431Z.json` (`call.billedCostUsd`, `accountUsageDeltaUsd`, `note`); `artifacts/evidence/demo-run-2026-09-18T22-01-13-583Z.json` (`measuredModelCostUsd`); `npm run model-check` (`scripts/model-check.ts`). |
| V25 | In the live run the publication gate rejected a real model draft and published the revision. The first draft (`deepseek/deepseek-v4-pro`) did not match the Brief schema; the routed endpoint did not enforce strict structured output. The gate answered REJECT on rule `SCHEMA`. The one permitted revision (`openai/gpt-5.6-terra`) passed all 11 rules and the gate answered PUBLISH. One run. No rejection rate can be inferred from it. | `artifacts/evidence/demo-run-2026-09-18T22-01-13-583Z.json`: timeline entries "Publication gate: REJECT (SCHEMA)", "revise brief after gate rejection" and "Publication gate: PUBLISH"; step `gate` lists the 11 findings. |
| V26 | In the purchase in V20 a real facilitator timeout was recorded as `PAYMENT_UNKNOWN` and reconciled from the chain, with one signature and one `settle` call. With `syncSettle: true`, `settle` returned `status: "timeout"` together with a transaction hash 1.87 s after it was called. Order trail: QUOTED → PAYMENT_PENDING (22:03:37.692Z) → PAYMENT_UNKNOWN (22:03:39.560Z) → PAID (22:03:51.434Z, reconciled: Transfer of 3000000 base units found in block 41310183) → DELIVERING → DELIVERED. Nothing was delivered while the outcome was unknown. The buyer re-sent the same authorization after the 503. One observation, not a rate. | `artifacts/evidence/delivery-ord_8a2102084ad69dab.json` (`payment.chainVerified: true`, `chainBlockNumber: 41310183`, `note`); `artifacts/integration/payment-ord_8a2102084ad69dab.json`; SPONSOR_FEEDBACK SF-7. The event trail with its timestamps is in the seller's order ledger (`GET /api/orders/ord_8a2102084ad69dab`; stored under `data/`, which is not committed), so the committed artifacts show the outcome, not the trail. The same path under test: `checkout.test.ts` "treats a settle timeout as unknown…"; the retry loop is in `scripts/buy-brief.ts`. |
| V27 | The paid resource answers `POST` as well as `GET`, and `/api/v1/briefs/latest` is one stable paid address whose challenge names itself as `resource.url`. A buyer quoted one Brief there still receives that Brief if a newer one is published before they pay, with one `settle` call; a withdrawn Brief is never offered; with nothing on sale the reply is `404` and no challenge. | `apps/api/test/listing.test.ts` (9 tests, fixture rail; the quoted-Brief test fails when the payment-to-quote lookup is removed). On 19 Sep 2026 `curl -i -X POST http://localhost:4499/api/v1/briefs/latest` against a local server on the OKX testnet rail returned `402` with a `PAYMENT-REQUIRED` header carrying `x402Version` 2, `resource`, and `accepts[0]` with `scheme`, `network` `eip155:1952`, `asset`, `amount` `3000000`, `payTo`, `maxTimeoutSeconds` 300 and `extra`. Localhost only; no payment was made through this address on a real rail. |
| V28 | With `AUTO_DESK=true` the server scans and investigates without an operator: one signal per tick, only a signal never investigated before, never while another run is active or while the model or rail is not ready, and never more than the daily ceiling. | `apps/api/src/desk/autoDesk.ts`; `apps/api/test/auto-desk.test.ts` (7 tests, fixtures). It has never been run against live sources or for longer than a test. |
| V29 | The service is deployed: Railway, one instance, a volume at `/data`, health check on `/api/health`, built from the repository's Dockerfile, deploying from `main`. | https://bullseye-production-5d0c.up.railway.app/api/health (public). First deployment 19 Sep 2026 from commit `1ca415d`. When this row was written the model key and the OKX credentials had not been entered on the host, so nothing was on sale there and no payment had been made through it. See B3. |
| V30 | On a public deployment nobody without the operator token can start paid work: scan, investigate and reconcile answer `403` (or `401` with a token configured), a production build never treats itself as local, and the paid routes are rate-limited per client address. | `apps/api/test/public-deployment.test.ts` (7 tests), `apps/api/test/payment-abuse.test.ts`; `https://bullseye-production-5d0c.up.railway.app/api/health` reports `operatorRoutes`. The per-address keying behind Railway's proxy was checked on the deployment on 19 Sep 2026 (see HANDOFF). |
| V31 | A sold Brief cannot be fetched with data copied from the chain. On the paid resource an existing order is answered only to the exact payload that opened it plus a claim token (since 19 Sep the token alone also opens that one order on the order routes: V34); a consumed authorization is never taken as payment without the quoted `Transfer` in the transaction that consumed it; an interrupted settlement is recovered without settling twice; a payload that cannot pay the quote costs no order row and no facilitator call. | `apps/api/test/payment-abuse.test.ts` (17 tests; the six that target the first fix fail on the code before it). Found by a review of the service as a public deployment and a second review of the fix; none of it has been attacked on a live rail. The transfer search was run once against the real settlement of 18 Sep 2026 (found, block 41310183) and once with an unknown nonce (not found). The residual race for buyers who do not choose a token is described in ARCHITECTURE, "Who an order is answered to". |
| V32 | On the deployed service, with the model key and OKX credentials in place, the auto desk ran a live investigation with no operator, and the gate refused to publish it. 19 Sep 2026, 10:16 to 10:18 UTC, signal AVGOx, investigation `inv_bdb36ac9f9a8509f`: 10 tool calls, 8 checks passed and 1 unknown, 5 model calls all routed to `deepseek/deepseek-v4-pro`, billed $0.058282. The draft and the one permitted revision both failed the gate's `SCHEMA` rule (prose strings where the schema requires arrays of claims), so the run ended REJECTED: nothing was published and nothing was offered for sale. | `https://bullseye-production-5d0c.up.railway.app/api/investigations/inv_bdb36ac9f9a8509f` (public, as long as that database lives). The routed endpoint accepted `response_format` and did not enforce it, as on 18 Sep (V25), and until this run the schema was sent only there. The writing prompt now spells the shape out (`apps/api/test/prompts.test.ts`). Whether that is enough for this model is not yet known: two rejected runs out of two drafts by it is two observations, not a rate. |
| V33 | The deployed desk published a Brief with no operator and offers it for sale at the stable address. 19 Sep 2026, 10:23 to 10:25 UTC, signal ESx, investigation `inv_f117ffa0cac381a6`: 10 tool calls, 8 checks passed and 1 unknown, 5 model calls (`deepseek/deepseek-v4-pro`), billed $0.062523. The first draft had the right shape and was rejected on `NUMBERS_IN_TEXT_ARE_EVIDENCED`; the revision passed all rules and became `brf_9aac63b6842fb78e` (LIVE, HIGH). `POST https://bullseye-production-5d0c.up.railway.app/api/v1/briefs/latest` then returned `402` and a `PAYMENT-REQUIRED` header with `x402Version` 2, `resource.url` equal to that address, and `accepts[0]` `exact` / `eip155:1952` / `3000000` / `maxTimeoutSeconds` 300: OKX's listing self-test, on the deployed address. | `https://bullseye-production-5d0c.up.railway.app/api/investigations/inv_f117ffa0cac381a6` and `https://bullseye-production-5d0c.up.railway.app/api/v1/catalog` (public, for as long as that database lives). One run. It followed V32 and the prompt change; one published run after one rejected run says nothing about how often this model will pass. No payment has been made through the deployed address. |
| V34 | A purchase from the browser is the buyer's own and can be recovered without paying twice. The page chooses a claim token before anything is signed and sends it with the payment; it keeps the immutable quote, the token, the signed authorization and the order id (no key). It asks the wallet to sign only when no earlier purchase of that Brief is unresolved. A second click, a lost paid response, a reload, a new quote requested meanwhile, or a refused write to storage all lead back to the same authorization or to `GET /api/orders/:id/delivery` with the token; an unknown outcome stays unknown, is re-read from the chain a bounded number of times and then waits for the buyer; two browsers buying one Brief get separate orders and cannot read or collect each other's; a wrong token is refused; a declined signature, a wallet on another network, insufficient funds, an expired quote and a missing wallet each say that nothing was charged, and the missing-wallet notice points at the agent buyer. The console reads the buyer's own order with the token and no longer reads the list of all orders. | `apps/web/src/checkout/purchase.ts`, `usePurchase.ts`, `components/CheckoutNotice.tsx`, `wallet.ts`, `signer.ts`; `apps/web/src/__tests__/checkout.test.ts` (20 tests; removing the "earlier purchase is unresolved" guard fails three of them); `apps/api/test/buyer-access.test.ts` (12 tests; making the token check always true fails four); `tests/e2e/golden-path.spec.ts` tests 3 to 7 (unknown payment, two browsers, reload after delivery, a paid response dropped on the way back, wallet refusals and no wallet). Known limit: a browser that keeps no storage at all and is reloaded before delivery has lost its token and its authorization; the page warns when that is the case. Offline only: see B5. |
| V35 | The agent buyer finds its purchase again instead of signing twice. `npm run buy` writes its claim token and the one signed authorization under `data/purchases/` (git-ignored) before sending, and sends nothing if that write fails; a run that finds an undelivered purchase of the same Brief by the same payer re-sends that authorization; `--collect <orderId>` fetches a purchase again with the token and needs no key; `verify-payment` reads the order with that token. | `scripts/purchase-journal.ts`, `scripts/buy-brief.ts`. Run once on 19 Sep 2026 against the offline server (fixture rail, a public development key): a purchase left PAYMENT_UNKNOWN, the buyer process killed after its first retry, the same command run again printed "resuming … Nothing new will be signed" and was delivered; the seller's ledger showed one order with one PAID event; `--collect` and `verify-payment` then worked with no key in the environment. No automated test covers the script, and its output was not kept as an artifact because fixture deliveries are not evidence. |
| V36 | Gate 2.0.0 binds every figure in a draft to typed evidence. A unit registry owned by code gives each numeric evidence key a unit class. In a claim, each figure written in digits must bind to one of that claim's declared quantities: the unit as written must fit ("%" only for percentages, "$"/"USD" only for USD, a fraction only in its typed percentage form), the sign must match (a direction word may carry it on signed fields only), and the figure must be the value correctly rounded with at least two significant digits. Counts of checks, evidence items and reads are computed by the gate, never taken from evidence. There is no small-integer exemption. Headline, rationale, unknowns, limitations and conflicts bind by unit class, sign and precision only, which is weaker and is said so in the code. Timestamps match on component boundaries. A spelled-out figure with a unit is refused. While a core check is not PASS, a headline or rationale that announces a confirmation fails `FAILED_CHECKS_DISCLOSED` even when the conflict is listed. The gate is still pure code: no check was replaced by a model. | `apps/api/src/gate/numericGrounding.ts`; `apps/api/test/gate-grounding.test.ts` (55 tests, each a full-gate test that changes one thing in a draft that otherwise publishes; 33 of the first 49 fail against the 1.0.0 logic, 23 because the old gate published the draft; the other 6 were added after a review of 2.0.0 itself and fail against it as first written). Before the change a full-gate reproduction published drafts containing an unsupported "$10", an unsupported "12%", "-13 USD" against evidence of 13, "50 USD" against a rate of 0.5, a block number quoted as a holder count, and "1" for a multiplier of 1.0033. The recorded live Brief (V18) passes 2.0.0 with its own evidence and checks. **No live model draft has been judged by 2.0.0**, so its effect on how often a first draft is rejected is not known; the writing prompt now lists the unit for each value key from the same registry. What it does not catch is listed in `docs/EVALS.md`. |
| V37 | The read routes separate three readers. Anyone gets the public view: that each step of an investigation happened, when and whether it worked; the issuer's public figures; which on-chain reads exist and how they were sourced, with the chain's numbers left out; call counts without cost. A run keeps its gate findings and its on-chain figures only when its signal has no Brief and no run in progress, because only then is nothing sold from what it read: every run of a signal reads the same event at the same blocks, so an earlier rejected run is closed once a later run of that signal publishes, and a run that is still going is closed for the minutes before its Brief exists. For a protected run the public view does not say which checks failed or how many passed; the detail of a rejected first draft is withheld once a revision of it is on sale. A buyer reads, reconciles and collects one order with its claim token; a missing order and a wrong token get the same answer; the token is never taken from the URL. `GET /api/orders` is an operator route. Diagnostics, the full view, go to the operator, a localhost development desk, and `VIEWER_TOKEN`, which reads and can start nothing and opens no order. The public activity feed names no order or quote. `GET /api/commerce/summary` is counts by state and Brief, labelled, with no identifier in it. | `apps/api/src/http/projections.ts`, `apps/api/src/http/app.ts`; `apps/api/test/access-projections.test.ts` (14 tests: the leak test reads every public route and looks for each evidence summary, block number and sentence of the Brief, and fails when the projection is switched off), `apps/api/test/buyer-access.test.ts`. Deployed on 19 Sep 2026 and read back from the public address (V43); until that afternoon the deployment served evidence summaries and `GET /api/orders` to everyone. |
| V38 | Desk-level economics count each investigation once. `GET /api/desk/economics` totals research per investigation whatever came of it (published, rejected, stopped, unsold), keeps measured and upper-bound cost apart, counts estimated delivery allowances per paid order, and counts revenue only for mainnet orders whose transfer was read back from the chain; test payments are reported separately and never as revenue. Summing per-order receipts would count a Brief's research once per sale and unsold research never. | `apps/api/src/economics/deskEconomics.ts`; `access-projections.test.ts` "desk economics" (4 tests, synthetic usage and orders). It has produced no figure from live data. |
| V39 | A restart keeps an order and the buyer's way back to it. Over a database file, a Brief, its quote and two orders survive the process; an order left PAYMENT_UNKNOWN is still unknown, still answered only to its claim token, and is reconciled and delivered by the new process with no `settle` call; a token the old process issued still opens its order; a settled authorization re-sent afterwards is recognised and not charged again. | `apps/api/test/restart.test.ts` (1 test, a temporary SQLite file, fixture rail). The Railway volume has not been tested this way. |
| V40 | From a fresh clone with nothing but the lockfile, the whole offline suite passes. On 19 Sep 2026, commit `6a4667f`, Windows 11, Node 26.7.0, npm 12.0.2: `npm ci`, `npm run typecheck` clean, `npm test` API 236 passed and 1 skipped (it needs the built desk, which a clean clone does not have until `npm run build`), web 53 passed, `npm run build`, then `npm run test:e2e` 7 passed with `PW_CHANNEL=msedge`. | Run by hand in a temporary clone; the output was not kept as an artifact. Without `PW_CHANNEL` the browser tests need `npx playwright install chromium` first. On Windows the clone path has to be short: the longest tracked path is 121 characters and the default limit is 260. A CI workflow runs the same scripts on Node 22 and 24 (`.github/workflows/ci.yml`); see P7. |
| V41 | What the deployed service showed on 19 Sep 2026 is kept in the repository: health, the three unattended runs, the run the gate rejected (V32, in full), the run it published (V33, as the public view only, so nothing the Brief sells is in the repository), the catalogue, and the unpaid listing self-test with its decoded challenge. | `artifacts/evidence/deployed-snapshot-2026-09-19.json`, taken with unauthenticated `GET` requests and one unpaid `POST`. No payment was made and no operator route was called. It shows what the service answered, not that anyone can buy from it. |
| V42 | Before it was merged, the fixes work was reviewed by readers who had not written it, in five areas (the seller's order code, what a visitor can read, the gate's number rule, the browser purchase, and whether production would start), and each finding was checked by a second reader who tried to refute it. Nine findings stood and all nine were fixed, each with a test that fails on the code as it was: after a failed payment a retry on the same quote re-sent the dead authorization under a new claim token and locked the buyer out of that Brief; a second tab holding a stale record could sign again; an earlier rejected run of a signal stayed readable in full after a later run of that signal published; two overlapping collects of one order got one false `500`; the public view gave away how many checks passed; and in gate 2.0.0 a figure after "version" was read as a count (so a wrong version could publish), an en dash was not read as a minus sign (so a flipped sign could publish), a time of day ending in Z was rejected, and "prior" near a percentage flipped its sign. The production-start review found nothing. | `apps/web/src/__tests__/checkout.test.ts` ("asks the wallet again on the same quote…", "two tabs over one localStorage"), `apps/api/test/access-projections.test.ts` ("closes an earlier rejected run…", "does not say which checks failed…"), `apps/api/test/buyer-access.test.ts` ("delivers to both of two collects…"), `apps/api/test/gate-grounding.test.ts` (six tests under sign, counts and activation timing). A review finds what its readers thought to look for; it is not proof that nothing else is wrong. |
| V43 | The fixes are on `main` and deployed, and the deployed service serves the new read contract while still selling. 19 Sep 2026: `main` fast-forwarded from `84e4a22` to `826820d`; Railway deployment `9840ee30` built from that commit and started with the existing volume (three investigations and two Briefs written by the older code loaded unchanged; the auto desk idled at its daily cap, so no investigation was interrupted). Read back from the public address without credentials: `GET /api/orders` answers `403`; an unknown order id and a missing token answer `403`; the investigation list and the published run are the PUBLIC view (evidence, check, model and tool details null, usage without cost, chain figures null with the issuer's figures kept); the rejected run's chain figures are readable; `/api/commerce/summary` and `/api/desk/economics` answer as labelled aggregates (three investigations, measured research $0.169, revenue $0, no orders); the public activity feed names no order or quote; two Briefs are on sale; the unpaid listing self-test answers `402` with a `PAYMENT-REQUIRED` header for `3000000` base units on `eip155:1952` to the same pay-to address as before; the desk page is served. | `artifacts/evidence/deployed-check-2026-09-19.json` (19 checks, all passed; unauthenticated `GET`s and one unpaid `POST`). It is not a purchase: nobody has bought from the deployed address, no unknown payment has been recovered there, and gate 2.0.0 has not yet judged a live draft. |
| V44 | Every push to `main` and every pull request is tested without secrets. The first run, on `826820d`, passed: typecheck, unit and integration tests and the build on Node 22 and on Node 24, and the seven browser tests on Linux with Playwright's own Chromium. | `.github/workflows/ci.yml`; GitHub Actions run `35458243392` (jobs `test (22.x)`, `test (24.x)`, `browser`: success). One run. |

| V45 | The Market Desk's figures are computed by exact decimal arithmetic, not in floating point and not by a model. Integer arithmetic on `BigInt` at 36 decimal places, from decimal strings, rounded once at a precision the caller states. A value that does not parse is refused, not repaired; more precision than the scale holds is refused, not truncated. | `apps/api/src/market/decimal.ts`; `apps/api/test/market.test.ts` ("decimal arithmetic", 6 tests), including that `0.1 + 0.2` is exactly `0.3` here and that `""`, `" 1"`, `"1,024.5"`, `"1e21"`, `1e21` and `5e-7` all throw. No model is reachable from `market/`. |
| V46 | For the real QSRx rebase of 18 Sep 2026 the desk states the balance change as `+0.665165779779 %`, the stale-cache understatement as `−0.66077056013 %`, the double-application overstatement as `+0.665165779779 %`, a position value of `+48.39 USD` on a stated 100 tokens, and an implied reinvestment price of `73.2900 USD` against a reference price of `72.75`. A rebase percentage is labelled a balance change, never a price return. | `apps/api/test/market.test.ts` ("what the rebase does, from the real QSRx event", 6 tests) against the numbers in `artifacts/recorded/xstocks-2026-09-18/`; `tests/e2e/market-desk.spec.ts` asserts the same figures in a browser. `docs/MARKET_DESK.md` shows the derivation of each. |
| V47 | The double-application error is computed off the multiplier's level, not off the size of the change, so the two diverge when a multiplier was already above 1. On the recorded VGKx rebase the rebase is `+0.1741971579 %` and the double-application error is `+1.116517183338 %`. | `apps/api/test/market.test.ts` ("prices the double application of the multiplier off its level"); `tests/e2e/market-desk.spec.ts` ("the insufficient-data fallback") asserts both figures on the recorded VGKx event in a browser. |
| V48 | The desk refuses to show a figure whose inputs are missing or stale, names what is missing, and never treats an unknown cost as zero. No spread and no net edge has ever been shown: the permitted sources publish no bid, ask, size, expiry, venue or fee schedule. Established from live responses, not from documentation. | `npm run market-probe`, artifact `artifacts/integration/market-probe-2026-09-20T13-17-24-494Z.json` (10 checks, 4 blocked; `/quote`, `/quotes`, `/orderbook`, `/book`, `/depth` all 404; `price-data` returned `{"quote": null}` while the market was closed). `apps/api/test/market.test.ts` ("missing and stale inputs", "costs and the absence of a spread", 9 tests); `tests/e2e/market-desk.spec.ts`. Blockers written up as SF-8 to SF-11. |
| V49 | The Market Desk changes no access rule. A visitor reading a signal that may still sell gets the issuer's public figures and no chain values — not the reads, not the block numbers, and not any figure derived from them — while still being told how many reads exist. | `apps/api/test/market.test.ts` ("the paywall boundary", 3 tests), including that a chain value the issuer never published appears nowhere in a public body, by value or block number, and that the operator's view does carry it. It reuses `projectChain` from `apps/api/src/http/projections.ts` unchanged. |
| V50 | Opening the Market Desk makes no external call and spends nothing. `GET /api/market/:signalId` reads only stored evidence and stored chain reads. | `apps/api/src/http/app.ts` (the route takes nothing but the container's stores and `transport.now()`); `apps/api/src/market/marketView.ts` has no adapter or transport import. The 36 API tests run with no network. |

## Partial

| # | Claim | What exists | What is missing |
| --- | --- | --- | --- |
| P4 | "The investigator can also run on Anthropic's API." | `SYNTHESIS_PROVIDER=anthropic` makes the factory in `apps/api/src/research/provider.ts` select the Anthropic provider in `apps/api/src/research/model.ts` (tool-use loop, structured output, usage priced at list rates as `MEASURED_USAGE_AT_LIST_PRICE`). The default is `openrouter`. | No run through it is recorded in `artifacts/`. The one live investigation used OpenRouter (V18). |
| P6 | "A stranger can buy a Brief from the browser." | The whole buyer path is built and passes in a real browser against the offline configuration (V34, V17). | No purchase has been made from a browser with a wallet extension, on a real rail, or on the deployed address. The only real settlement (V20) was made by the agent buyer against a localhost server. |
| P5 | "Payments settle on X Layer mainnet." | `PAYMENT_RAIL=okx-mainnet` selects `eip155:196` in `apps/api/src/commerce/rail.ts`, and the receipt counts a payment as revenue only when it is chain-verified on mainnet (`apps/api/src/economics/receipt.ts`). | Never run. The one settlement was on testnet (V20). |

## Blocked

| # | Item | Exact blocker |
| --- | --- | --- |
| B1 | Smallest documented OKX test payment (mock merchant). | The OKX client SDK cannot parse the mock merchant's challenge, which arrives in the body with no `PAYMENT-REQUIRED` header (SPONSOR_FEEDBACK SF-1): `artifacts/integration/mock-merchant-2026-09-18T19-35-31-514Z.json`, "FAILED before signing: Invalid payment required response". Funds are no longer the blocker: read with `npm run wallet` on 18 Sep 2026, the buyer wallet held 10 USDC_TEST (`0xcb8bf24c6ce16ad21d707c9505421a17f2bec79d`), the mock merchant's asset. The step is still BLOCKED in `artifacts/integration/spike-2026-09-18T21-37-16-168Z.json`. The EIP-712 version mismatch on USDC_TEST (SF-2) is therefore still untested end to end. |
| B3 | Listing on OKX AI as an A2MCP pay-per-call service. | The endpoint side is built and passes OKX's documented self-test on localhost (V27), the service is deployed (V29) and the self-test passes on the deployed address (V33). Still needed, and only the owner can do it: register the ASP with an Agentic Wallet and submit the listing. OKX's listing example uses mainnet `eip155:196`; its documentation does not say whether a testnet endpoint is accepted. Not attempted. |
| B5 | A browser purchase and an agent purchase against the deployed HTTPS address, including an unknown-payment recovery. | The code for it is merged and deployed (V43). Still needed: a decision on `VIEWER_TOKEN` for the wall display, which without one shows steps and counts and no per-run cost or on-chain figures; the owner's approval of a bounded testnet spend; and the buyer key, which only the owner holds (`.env` in the main checkout; the buyer wallet held 7 test USD₮0 on 18 Sep). An unknown outcome cannot be forced on the real facilitator: if it does not happen by itself (it did once, V26), that case stays demonstrated on the fixture rail only, and must be labelled so. |
| B4 | Demand evidence. | No interviews or usability sessions have been run. `docs/DEMAND.md` contains the plan and an empty results table. |

## Not claimed

Guaranteed alpha or profit · investment advice · autonomous trading · customer revenue · that
testnet or fixture payments are revenue · that estimated contribution is profit · evidence of
demand · mainnet settlement · Brief quality, a gate rejection rate, a settlement success rate, a
typical cost per Brief or a time per investigation (one live investigation and one live
settlement exist; neither is a sample) · detector recall (nothing measures events it misses) ·
a tradable spread, an arbitrage, a realisable amount or a net edge on any event (no source Bullseye
may read publishes a bid, an ask, a size or an expiry; every such figure is labelled INSUFFICIENT
DATA and the missing inputs are named) · that the Market Desk's reference figures could be
transacted at (a reference price has no side, no size and no venue) · that the implied
reinvestment price is a mispricing (most of the difference is the nineteen hours between the two
observations, and the figure says so) · shares backing a token (the issuer's supply figures are not
in one unit or scope) · that a forward tracker exists ·
real-time detection or early warning (no latency has been measured; the detector polls; the one
live Brief was first detected 21 h 29 min after the event took effect, and the desk labels such a
case retrospective) · that a browser purchase has been made with a real wallet · that anyone has
bought from the deployed address · that gate 2.0.0 has judged a live model's draft · comprehensive market
coverage (one signal type, one issuer, one chain) · that a content hash proves a source was
truthful (it proves the Brief and the stored responses have not changed) · marketplace approval.

## The strongest honest statement today

"On 18 September 2026 Bullseye detected a QSRx dividend rebase from the issuer's public record,
confirmed it independently against X Layer contract state, and took it once through the whole
path: a budgeted live-model investigation that cost $0.071725 as billed against a $0.60 ceiling,
a deterministic publication gate that rejected the first draft and published the revision, and
an x402 purchase that settled through the OKX facilitator on X Layer testnet and was verified
on-chain in block 41310183. That is one investigation and one testnet settlement. The payment
was made with test tokens and is not revenue, and nothing here measures quality, reliability or
demand."
