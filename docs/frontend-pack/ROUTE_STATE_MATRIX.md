# Route × state matrix
Fill observed results at current HEAD. Every row starts NOT RUN here. Dates/screenshots supplied previously are not current render coverage.

| Surface | Primary task | Must exercise | Special constraint |
|---|---|---|---|
| `/market` | Understand/select event | populated, no events, request error, loading, older event, missing reference | Stable picker; no forced investigation by visitor |
| `/market/<signalId>` | Inspect exact event | valid ID, unknown ID, switched event, Back/Forward/reload | URL and selected content agree |
| `/?brief=<briefId>` or documented canonical replacement | Obtain exact report | direct link, valid published, unknown, withdrawn, no matching event | No silent first/default report substitution |
| Existing `/` desk | Read/recover selected brief | unselected, investigating, rejected, published/unpaid, paid/delivered | Preserve usePurchase and current public/operator split |
| Evidence drawer | Check a fact | public metadata, paid data, withheld, long URL/hash, keyboard | Focus lifecycle; no tooltip/ARIA leak of hidden values |
| Quote/checkout | Understand/authorize purchase | ready, no wallet, refused signature, wrong chain, expired quote, unavailable rail | Amount/token/network from verified terms, testnet labelled |
| Recovery | Receive what was paid for | pending, unknown, request lost, reload, claim retrieval, second buyer | No new authorization or double charge; no global order lookup |
| `/room` where served | Inspect operational proof | idle, daily cap, rejected draft, published, zero orders, aggregate economics | Stated diagram semantics; do not fake activity to populate it |
| Agent access | Read API instructions | real endpoint, missing config, unpaid402, authorised JSON | An API challenge is not a broken normal download |

## Viewport matrix
- 360×800 / 390×844 phones.
- 768×1024 tablet and 1024×768 landscape.
- 1280×720 and 1440×900 judged laptop sizes.
- 1920×1080 presentation rehearsal; not a substitute for smaller widths.
- 1199/1200/1336 or new breakpoint boundaries for grid regression.
- 200% zoom, reduced motion and failed web font load.

For every render record permission mode, source mode, payment mode, dataset/selected IDs, timezone, network availability, commit and route. Record ID-bearing proof privately if a buyer capability is visible; sanitize screenshots before publishing.

## Minimal report columns
`case | route | viewport | mode | steps | expected | observed | PASS/FAIL/NOT RUN | artifact | commit`

Focus interactions require keyboard testing; a screenshot cannot establish them. Lab throttling is a test setup, not real-user performance telemetry.
