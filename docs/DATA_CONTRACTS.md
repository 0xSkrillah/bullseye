# Data contracts

The schemas in `packages/domain/src` are the contracts; this page is a map. Everything that
crosses a boundary (source response, model output, HTTP body, stored row) is parsed with Zod.

## External sources → adapters (`apps/api/src/adapters/xstocks.ts`)

Strict on the fields Bullseye depends on, tolerant of additions. Nothing is patched over: a value
that fails validation is never repaired or defaulted.

- A single-object response that does not match raises `SchemaMismatchError` and the request fails
  (`502 source_schema_mismatch` on an HTTP route).
- The corporate-action history is a list and is validated record by record. A record that fails is
  left out and returned in `rejected` (`index`, `eventId` if readable, `reason`); the other records
  are used. `SchemaMismatchError` is still raised when more than half the records of a page fail,
  or when the page itself has no `nodes` array.
- A rejected record is surfaced, not dropped silently. The scan lists it in `skipped` and trusts no
  version of its event until it parses. Inside an investigation `get_corporate_action` throws if
  the symbol's history contains a rejected record for the signal's event, or one with no readable
  `eventId`.

| Endpoint (xStocks API v2, public) | Schema | Fields relied on |
| --- | --- | --- |
| `GET /public/corporate-actions/history` | `{ nodes: unknown[] }`, each node parsed as `XsCorporateAction`; failures go to `rejected[]` | `eventId`, `version`, `xstockSymbol`, `caType`, `effectiveTimeUtc` (nullable; null on cancelled actions), `multiplierOld`, `multiplierNew` (both nullable), cashflows, `withholdingTaxRate`, `createdTimeUtc`, `status` (Initial, Corrected and Cancelled seen on 2026-09-18), `notes` (optional, nullable; a cancellation names the cancelled version here: `[CANCELLED v2] reason`) |
| `GET /public/assets/{symbol}` | `XsAsset` | `symbol`, `name`, `isin`, `underlyingSymbol`, `deployments[].network/address` |
| `GET /public/assets/{symbol}/multiplier?network=XLayer` | `XsMultiplier` | `currentMultiplier`, `newMultiplier` |
| `GET /public/proof-of-reserves/{symbol}` | `XsProofOfReserves` | `timestamp`, `sharesHeld`, `circulatingSupply`, `holdings[]` |
| `GET /public/assets/{symbol}/price-data` | `XsPrice` | `quote` |
| `GET /public/system/status/{symbol}` | `XsTradingStatus` | halt flags |
| `GET /public/assets/{symbol}/total-supply`, `/circulating-supply` | `XsSupply` | `value` |

X Layer reads (`xlayer.ts`): `multiplier()`, `totalSupply()`, `decimals()` on the xStock token at
a given block, plus block headers. Values are returned as decimal strings and block references so
they are JSON-safe and hash the same before and after storage.

Model provider (`apps/api/src/research/openrouter.ts`): the chat-completions response is parsed
with Zod. Fields relied on: `choices[0].message.content`, `choices[0].message.tool_calls[]` (`id`,
`function.name`, `function.arguments`), `choices[0].finish_reason`, the top-level `model` (the
model that answered) and `usage` (`prompt_tokens`, `completion_tokens`, `cost`, `is_byok`,
`cost_details.upstream_inference_cost`, `prompt_tokens_details.cached_tokens` and
`cache_write_tokens`). `usage` is read before anything that can throw. A 200 body that does not
match the shape raises `ModelCallError`. A 200 body that carries an error object raises
`ModelUnavailableError` for codes 401, 402, 403, 404 and 503 (the call is recorded at $0) and
`ModelCallError` for any other code. `ModelCallError` carries the reported usage, or a ceiling when
none was reported, so the call is still costed.

## Domain objects

| Object | File | Notes |
| --- | --- | --- |
| `Provenance`, `DataMode`, `weakestMode` | `provenance.ts` | `mode`, `source`, `url`, `fetchedAt`, `sha256` of the raw body, optional `note`. |
| `SignalEvent` | `signal.ts` | `id = sig_` + sha256(`eventId:version`)[0:16]; `category`, `asset` (symbol, X Layer token address, chain id 196), `observedAt`, `detectedAt`, `headline`, `reasonFlagged`, `facts`, `sources[]`, `provenance` (`mode`, `detector`, `detectorVersion`, `inputHash`). Only a version the issuer still stands behind becomes a signal. A later cancellation or replacement does not change the stored signal; it is recorded beside it in `signals.superseded_json` as `byVersion`, `reason` (`CANCELLED` or `REPLACED`), `status`, `notes`, `notedAt`. |
| `EvidenceItem`, `ConsistencyCheck` | `evidence.ts` | Stable ids (`EV-CA`, `EV-CHAIN-BEFORE`, …); flat `values` map; `observedAt`; `staleAfter`; provenance. `EV-CA` holds the signal's own version of the issuer record plus `newestVersion`, `newestStatus`, `supersededByVersion`, `supersededReason` (`CANCELLED` or `REPLACED`) and `supersedingNotes`; the last three are null while the version stands. Checks are PASS / FAIL / UNKNOWN with the evidence they used. There are 9: `CHK-ACTION-STILL-CURRENT`, `CHK-BEFORE-MATCHES-OLD`, `CHK-AFTER-MATCHES-NEW`, `CHK-LATEST-MATCHES-NEW`, `CHK-API-STATE-MATCHES-CHAIN`, `CHK-RESERVES-COVER-SUPPLY`, `CHK-ACTIVATION-ON-SCHEDULE`, `CHK-REBASE-SIZE-PLAUSIBLE`, `CHK-TRADING-NOT-HALTED`. The first 4 are the core checks (`CORE_CHECKS` in `apps/api/src/evidence/checks.ts`); a failed or unknown core check caps confidence at LOW. |
| `ResearchBudget`, `UsageRecord`, `CostBasis`, `StopReason` | `budget.ts` | One usage record per model or tool call with tokens, latency, cost and `costBasis`. `UsageRecord.model` is the model that actually answered a model call (null for tool calls); it differs from the requested id when a router chose it. `CostBasis` is one of `MEASURED_PROVIDER_BILLED` (the amount the provider reports it charged), `MEASURED_USAGE_AT_LIST_PRICE` (reported tokens × list price), `UPPER_BOUND_AT_PRICE_CAP` (charge unknown; priced at the configured price cap; a ceiling, not a measurement), `NO_MARGINAL_PRICE` (tool calls, and a model call that never reached a model) and `FIXTURE` (test double; not a real cost). |
| `BriefDraft` | `brief.ts` | The only part a model writes: headline, three claim sections, confidence, unknowns (≥1), conflicts, limitations (≥1). Each claim lists `evidenceIds` and `quantities` (`label`, `value`, `unit`, `evidenceId`, `valueKey`). `unit` is free text in the schema; the gate decides whether it names the unit registered for that evidence key (`VALUE_UNITS` in `apps/api/src/gate/numericGrounding.ts`; see `EVALS.md`). |
| `GateResult` | `brief.ts` | Decision, gate version (`GATE_VERSION` in `publicationGate.ts`, `2.0.0`), confidence cap, one finding per rule that was evaluated (a draft that fails `SCHEMA` is judged no further, so its result holds two findings). `GateRule` has eleven rules. |
| `Synthesis` | `brief.ts` | `provider`, `model` (the id that was requested, e.g. `openrouter/auto`), `mode` (LIVE or FIXTURE) and optional `routedModels`: when `model` is a router, the sorted list of models it actually chose in this investigation. It is set only when at least one answering model was reported and the requested id is not among them. |
| `Brief` (`bullseye.brief/v1`) | `brief.ts` | Signal, draft, evidence, checks, gate, `synthesis` (a `Synthesis`), `dataMode`, disclaimer, `contentHash`. This JSON is the machine-readable product and the source the UI renders. |
| `QuoteTerms`, `Quote`, `Order`, `OrderState`, `ORDER_TRANSITIONS`, `PaymentEvidence` | `order.ts` | Terms are hashed at issue; an order stores the hash and can never point at other terms. |
| `EconomicsReceipt`, `CostLine` | `economics.ts` | `measuredCosts[]` and `estimatedCosts[]` are separate arrays with separate totals. Model calls at `MEASURED_PROVIDER_BILLED` or `MEASURED_USAGE_AT_LIST_PRICE` are measured lines; calls at `UPPER_BOUND_AT_PRICE_CAP` are an estimated line, never a measured one. |

## HTTP

The routes below are the code on the fixes branch (`HANDOFF.md` names it). They are not merged
to `main` and not deployed: on `main`, `GET /api/orders` and `GET /api/orders/:id` answer
anyone, reconcile is an operator route, the investigation routes are not projected, and
`GET /api/orders/:id/delivery`, `GET /api/commerce/summary` and `GET /api/desk/economics` do not
exist.

Four kinds of caller (`ARCHITECTURE.md`, "Who may read what" and "Who an order is answered to"):

| Caller | Sends | Routes |
| --- | --- | --- |
| Anyone | nothing | The buying path, the free previews, the two aggregates, and the public projection of the desk's read routes. |
| Buyer | `X-Bullseye-Claim: <claim token>`. A header only; a token in the URL is ignored. | `GET /api/orders/:id`, `POST /api/orders/:id/reconcile`, `GET /api/orders/:id/delivery`, for the one order the token belongs to. |
| Diagnostics | `Authorization: Bearer <OPERATOR_TOKEN or VIEWER_TOKEN>`; or nothing on a localhost desk with no `OPERATOR_TOKEN` configured | The same read routes, unprojected. `VIEWER_TOKEN` is accepted nowhere else. |
| Operator | `Authorization: Bearer <OPERATOR_TOKEN>`; or nothing on a localhost desk with no `OPERATOR_TOKEN` configured | `POST /api/signals/scan`, `POST /api/signals/:id/investigate`, `GET /api/orders`, `POST /api/_fixture/control` (fixture rail only); also `GET /api/orders/:id` and `POST /api/orders/:id/reconcile` for any order. Not order delivery. |

Operator routes are open on localhost. On any other `PUBLIC_BASE_URL` they answer
`403 operator_routes_disabled`, or with `OPERATOR_TOKEN` set `401 operator_token_required` to
anything but `Authorization: Bearer <token>`. In a production build (`NODE_ENV=production`) they
are never treated as local. A projected route never refuses: a missing or wrong bearer token gets
the `PUBLIC` view, and every projected response says which view it is in `audience` (`PUBLIC` or
`DIAGNOSTIC`).

Three fixed one-minute limits per client address answer `429 rate_limited` with `Retry-After` and
name themselves in `limit`: `paid_routes` on the paid resource, the quote route, order
reconciliation and order delivery (`PAID_ROUTE_RATE_LIMIT_PER_MINUTE`, 60), `payment_attempts` on
requests to those routes that carry a payment (`PAYMENT_ATTEMPT_RATE_LIMIT_PER_MINUTE`, 12), and
`api` on every `/api` route except the health check (`API_RATE_LIMIT_PER_MINUTE`, 600). Request
bodies are parsed after that limit; a body that is not JSON, or is over 100 kB, gets
`400 invalid_request`. A CORS preflight is granted the request headers it asks for (nothing uses
cookies), and `X-Bullseye-Claim` and `X-Bullseye-Order` are exposed to browser callers.

| Route | Caller | Request, response and status codes |
| --- | --- | --- |
| `GET /api/health` | Anyone | Data mode, synthesis readiness (`provider`, `model`, `ready`, `detail`; for OpenRouter the detail states the cost tier and the price cap), rail readiness, budget, `operatorRoutes` (`OPEN_ON_LOCALHOST`, `TOKEN_REQUIRED` or `DISABLED`), `diagnostics` (the same three values: who may read the `DIAGNOSTIC` view; `TOKEN_REQUIRED` when `OPERATOR_TOKEN` is set, otherwise `OPEN_ON_LOCALHOST` on a local desk, otherwise `TOKEN_REQUIRED` when `VIEWER_TOKEN` is set, otherwise `DISABLED`), `autoDesk` (`enabled`, and when enabled `intervalMinutes` and `maxInvestigationsPerDay`), SDK versions. |
| `POST /api/signals/scan` | Operator | Runs the detector. The result carries `skipped[]` (`symbol`, `reason`): assets that could not be read and issuer records rejected by the schema. `403 operator_routes_disabled`, `401 operator_token_required`. |
| `GET /api/signals` | Anyone | The feed. Each entry is `signal`, `superseded` and `investigation` (`id`, `status`, `stopReason`, `briefId`, or null); `superseded` is null, or `byVersion`, `reason` (`CANCELLED` or `REPLACED`), `status`, `notes`, `notedAt` once a scan has seen the issuer cancel or replace the signal's version. |
| `GET /api/signals/:id` | Anyone; projected | `signal`, and `investigation`: the latest run for the signal, projected exactly as in `GET /api/investigations/:id` and carrying its own `audience`, or null. `404 signal_not_found`. |
| `POST /api/signals/:id/investigate` | Operator | Starts an investigation, idempotent per signal: `202` when a run was started, `200` with the existing run otherwise. `404 signal_not_found`, `403 operator_routes_disabled`, `401 operator_token_required`. |
| `GET /api/investigations/:id` | Anyone; projected | `audience`, `investigation`, `budget`, `usage`. `budget` is today's configuration; `investigation.budgetAtStart` is what that run was held to, and `investigation.gateAttempts` is every `GateResult` the run produced, oldest first (both null for runs stored before they were kept). **`usage`, `DIAGNOSTIC`:** `modelCalls`, `toolCalls`, `measuredModelCostUsd` (only what a provider reported: billed amounts, or reported tokens at list price), `upperBoundModelCostUsd` (calls whose charge is unknown, carried at the price cap), `budgetSpentUsd` (what the governor counted against the ceiling: every basis), `costBases`, `costBasis` (the first model call's basis), `routedModels`, and `costsWithheld: false`. **`usage`, `PUBLIC`:** `modelCalls`, `toolCalls` and `costsWithheld: true`, nothing else. **`investigation`, `PUBLIC`:** the same object with `detail: null` on every `EVIDENCE`, `CHECKS`, `MODEL_CALL` and `TOOL_CALL` timeline entry; when the run may still sell (`briefId` is not null, or `status` is `RUNNING`), also `detail: null` on `GATE` entries, and in `gate` and every `gateAttempts` entry the `detail` of each failed finding is replaced by the sentence "Withheld from the public view: it is part of the Brief, or of the desk's own records." The rule is `mayStillSell` in `apps/api/src/http/projections.ts`. Only a run that finished with no Brief (`REJECTED` or `STOPPED`) keeps its `GATE` details and the detail of its failed findings; its `EVIDENCE`, `CHECKS`, `MODEL_CALL` and `TOOL_CALL` details are null like any other run's. Rules, `passed`, decision, cap, labels, times and evidence ids are never withheld. `404 investigation_not_found`. |
| `GET /api/investigations[?limit=]` | Anyone; projected | `audience` and `investigations[]`: every run, newest first (default 50, at most 100), without timelines: `id`, `signalId`, `symbol`, `status`, `stopReason`, `startedAt`, `finishedAt`, `briefId`, `gate` (`decision`, `confidenceCap`; null if no draft was judged), `draftsJudged`, and the same `usage` summary as the single view, in the same two shapes. |
| `GET /api/investigations/:id/chain` | Anyone; projected | `audience` and `chain`. The on-chain part of a rebase run as numbers: `chain` is null until a read exists, else `network`, `token`, `symbol`, `issuer` (`multiplierOld`, `multiplierNew` as the issuer's decimal strings, `effectiveTimeUtc`), `reads[]` (`key` BEFORE / AFTER / HEAD, `evidenceId`, `blockNumber`, `blockTime`, `multiplier` as a decimal string, `mode`), `activation` (`evidenceId`, `blockNumber`, `blockTime`, `mode`; null when none was found), `activationSearched` and `withheld`. Every value comes from the run's stored evidence; nothing comes from the Brief. **`DIAGNOSTIC`, or any reader of a run that finished with no Brief (`REJECTED`, `STOPPED`):** the numbers, `withheld: false`. **`PUBLIC`, for a run that has a Brief or is still `RUNNING`:** `withheld: true`; each read keeps `key`, `evidenceId` and `mode` with `blockNumber`, `blockTime` and `multiplier` null; `activation` keeps `evidenceId` and `mode` with `blockNumber` and `blockTime` null. `issuer` is the issuer's public record and is never withheld. `404 investigation_not_found`. |
| `GET /api/desk/status` | Anyone | The unattended loop: `enabled`, `intervalMinutes`, `maxInvestigationsPerDay`, `investigationsLast24h`, `lastTick` (`at`, `action` STARTED or IDLE, `reason`, `detail`; process memory, so null after a restart until the first tick), `nextTickAt`, `dailySpendCeilingUsd`, and `liveWindowHours` (how far back the detector looks). |
| `GET /api/desk/economics` | Anyone | Desk totals with every investigation counted once (`apps/api/src/economics/deskEconomics.ts`; see `ECONOMICS.md`). `label` (`AGGREGATE`), `note`, `investigations` (`total`, `published`, `rejected`, `stopped`, `running`), `research` (`basis`; `total`, `byOutcome.PUBLISHED` / `REJECTED` / `STOPPED` / `RUNNING` and `unsold`, each `runs`, `measuredUsd`, `upperBoundUsd`; `runsWithoutAPrice`), `sales` (`paidOrders`, `revenueOrders`, `revenueUsd`, `testOrders`, `testPaymentsUsd`, `unverifiedMainnetOrders`, `unverifiedMainnetUsd`, `note`), `delivery` (`basis` `ESTIMATED`, `perPaidOrderUsd`, `estimatedTotalUsd`, `note`), `estimatedContributionUsd`, `contributionNote`, `rail`, `isTestnet`. It carries no investigation, Brief or order id and no payer. It reads the 500 newest investigations and the 500 newest orders. |
| `GET /api/activity[?limit=]` | Anyone; projected, with no `audience` field | One feed of recorded events, newest first (default 50, at most 200): `kinds` lists the values of `kind` (`SIGNAL_DETECTED`, `SIGNAL_SUPERSEDED`, `INVESTIGATION_STARTED`, `GATE_DECISION`, `INVESTIGATION_PUBLISHED`, `INVESTIGATION_REJECTED`, `INVESTIGATION_STOPPED`, `QUOTE_ISSUED`, `ORDER_STATE`); each event has `at`, `kind`, `refId`, `symbol`, `summary`, `rail` (quote and order events only) and `state` (`ORDER_STATE` only). Summaries are assembled by code from stored fields. They never carry model text, a gate finding's detail, or what a facilitator or an RPC said about a payment. **`PUBLIC`:** the `refId` of `QUOTE_ISSUED` and `ORDER_STATE` events is a stand-in, `ref_` plus 16 hex characters, the same for the same quote or order every time, so rows still group. **`DIAGNOSTIC`:** the real quote and order ids. |
| `GET /api/briefs`, `GET /api/briefs/:id/preview` | Anyone | Free previews: headline, confidence, counts, the kinds of evidence (`evidenceKinds`), mode, hash. No findings. The preview response also carries `signal` (the issuer's public facts) and `withdrawn`: null, or the same object as `superseded` when the Brief's action was voided. `GET /api/briefs` still lists a withdrawn Brief and does not mark it. |
| `GET /api/v1/catalog` | Anyone | Machine-readable list of what is for sale, for agents. Withdrawn Briefs are omitted. `latest` names the stable paid address and its methods. |
| `POST /api/briefs/:id/quotes` | Anyone | Issue or reuse an immutable quote. This route does not check withdrawal; a quote for a withdrawn Brief cannot be paid, because the paid resource answers `410`. |
| `GET /api/commerce/summary` | Anyone; `latestReceipt` is `DIAGNOSTIC` only | What anyone may know about sales: counts, with no order id, quote id, terms hash, payer or transaction hash. `label` (`AGGREGATE`), `note` (on any rail but mainnet it says these are test payments: not revenue, not evidence of demand), `rail`, `isTestnet`, `countsAsRevenue` (true only when the configured rail is `OKX_X402_MAINNET`), `priceUsd`, `total`, `byState` (per order state: `count`, `chainVerified`), `byBrief[]` (`briefId`, `symbol`, `orders`, `furthestState`, and `count` and `chainVerified` of the orders in that state), `latest` (the newest order by update time: `at`, `state`, `rail`, `chainVerified`, `trail[]` of `at`, `from`, `to`; no reasons, no ids; null with no orders) and `latestReceipt` (`rail`, `countsAsRevenue`, `priceUsd`, `measuredTotalUsd`, `estimatedTotalUsd`, `estimatedContributionUsd` of that newest order; null for `PUBLIC`). `furthestState` ranks states by how far they are along the path to delivery: `PAYMENT_FAILED`, `QUOTED`, `PAYMENT_PENDING`, `PAYMENT_UNKNOWN`, `RECONCILIATION_REQUIRED`, `PAID`, `DELIVERY_FAILED`, `DELIVERING`, `DELIVERED`. It reads the 500 newest orders. |
| **`GET` or `POST /api/v1/briefs/:id[?quote=]`** | Anyone; an existing order only to its buyer | The paid x402 resource; both methods behave the same and any other answers `405`. `402` + `PAYMENT-REQUIRED`; with `PAYMENT-SIGNATURE`: `200` delivery envelope + `PAYMENT-RESPONSE`, `503 payment_outcome_unknown` (`message`, `orderId`, `state`, `orderUrl`, and `deliveryUrl`, the address to collect from with the claim token), `409 payment_in_progress`, or `402` on failure. `503 payment_rail_unavailable` when the rail cannot settle. `410 brief_withdrawn` (`briefId`, `detail`, `message`) when the Brief's action was voided and the request carries no payment or an authorization the ledger has not seen: no quote, no challenge, no settlement. An authorization the ledger already knows is answered from its order, so a buyer who already paid still gets delivery: but only to a request carrying the exact payload that opened the order, and with the claim token in `X-Bullseye-Claim` (`403 claim_token_required` otherwise; a payload that does not match gets a fresh `402`). The token is either one the buyer sent with the paying request (32 to 128 URL-safe characters; then it is always required) or the one the server returns in the `X-Bullseye-Claim` response header of `200`, `503` and `409` replies (then it is required once the Brief has been delivered, and the payload alone is enough before that). A payload that cannot pay the quote (no signature, wrong recipient or amount, malformed nonce, outside its validity window) gets a `402` whose error starts "payment rejected before verification" and opens no order. |
| **`GET` or `POST /api/v1/briefs/latest[?symbol=]`** | Anyone; an existing order only to its buyer | One stable paid address for the newest Brief on sale, optionally for one asset (`?symbol=QSRx`, or `{"symbol":"QSRx"}` in a POST body). Same replies as the row above, with `resource.url` set to this address, so a quote issued here is separate from a quote for the Brief's own address. `404 nothing_for_sale` and no challenge when no Brief is on sale; withdrawn Briefs are never offered. A request that carries a payment is matched to the quote it was signed against (an existing order first, then the quotes issued at this address, preferring the Brief named in the echoed resource description), so a buyer quoted Brief A still receives A after a newer Brief is published. |
| `GET /api/orders` | Operator | Every buyer's order, the 50 newest: `orders[]`, each `order` (with its event trail and payment evidence), `receipt` (the economics receipt) and `briefHeadline`. `order.paymentKey` is always null. `403 operator_routes_disabled`, `401 operator_token_required`. |
| `GET /api/orders/:id` | Buyer (`X-Bullseye-Claim`), or operator | `200`: `order`, `receipt`, `briefHeadline` for that one order; `order.paymentKey` is null. `403 claim_token_required` (`error`, `detail`) for a missing or wrong token and, with the same body, for an order that does not exist. `404 order_not_found` is only ever seen by the operator. The token is the one the buyer sent with the paying request, or, when the buyer sent none, the one the server returned in the `X-Bullseye-Claim` response header. |
| `POST /api/orders/:id/reconcile` | Buyer (`X-Bullseye-Claim`), or operator | No body. Re-examines an order in `PAYMENT_UNKNOWN` or `RECONCILIATION_REQUIRED` against the facilitator's record and the chain, and never calls `settle`; an order in any other state is returned unchanged. `200` with the same shape as `GET /api/orders/:id`. `403 claim_token_required` and `404 order_not_found` as above. Counted under `paid_routes`. |
| `GET /api/orders/:id/delivery` | Buyer (`X-Bullseye-Claim`) only | The Brief again for a buyer who holds the claim token but no longer the signed authorization. Takes no payment header, never settles, never issues a quote or a `402`. `200`: the delivery envelope with `X-Bullseye-Order` and no `PAYMENT-RESPONSE`; it counts as a delivery once written. An order whose outcome is unknown is reconciled first. `403 claim_token_required` for a missing or wrong token and for an order that does not exist (the operator's token does not open this route). `409 payment_in_progress` (`orderId`, `state`; `Retry-After: 3`) while settlement is running. `409 payment_failed` (`orderId`, `state`, `detail`) for a failed payment: a state, not a challenge. `503 payment_outcome_unknown` (`Retry-After: 10`) while the outcome is still unknown. `404 brief_not_found` if the Brief is gone, `500 delivery_failed` if the stored Brief no longer matches the quoted content hash. Still answered after the Brief was withdrawn from sale. Counted under `paid_routes`. |

Delivery envelope (`bullseye.delivery/v1`): `orderId`, `state`, `quote` (id, terms hash, terms),
`payment` (rail, payer, tx hash, facilitator status, chain verification), `brief`.
